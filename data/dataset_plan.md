# Dataset Plan

We will use a small open-domain QA dataset subset (e.g., SQuAD) for retrieval and reasoning. This is practical for an academic MVP and allows for evaluation with standard metrics.

Synthetic multi-step datasets are considered for future work if time permits.
